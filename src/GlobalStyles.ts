import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
    page: {
        flex: 1,
        backgroundColor: 'white',
    },
    boldText: {
        fontWeight: 'bold',
    },
});
